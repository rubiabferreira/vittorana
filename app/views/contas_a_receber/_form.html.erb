<%= form_for(@conta_a_receber) do |f| %>
  <% if @conta_a_receber.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@conta_a_receber.errors.count, "error") %> prohibited this conta_a_receber from being saved:</h2>

      <ul>
      <% @conta_a_receber.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

   <fieldset>
    <legend>Identificação</legend>
      <div class="row">
        <div class="small-12 medium-6 columns">
    <%= f.label :cliente_id , 'Selecionar Cliente' %>
    <%= f.collection_select(:cliente_id, Cliente.all, :id, :nome, { prompt: 'Escolha o cliente'}) %>
  </div>
  </div>


 <div class="row">
  <div class="small-12 medium-6 columns">
    <%= f.label :vencimento , 'Vencimento' %>
    <%= f.text_field :vencimento %>
  </div>
  </div>


 <div class="row">
  <div class="small-12 medium-6 columns">
    <%= f.label :valor, 'Valor' %>
    <%= f.text_field :valor %>
  </div>
  </div>

  </fieldset>


  <div>
    <%= f.submit 'Salvar' , class: 'button' %>
  </div>
<% end %>
